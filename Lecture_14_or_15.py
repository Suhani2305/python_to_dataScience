"""Central Tradencies:
    mean , median and Mode 
median is more statistics"""

"""negativity should not impact positivity that why we square in variance method
line of best fit = minimum RMSE"""


t"""tail heavy histogram:
    bell shaped curve"""
    
"""Training and test set:
    training set: generally conatin 80% of the data
    test set: generally contain 20% of the data
    test set should be randomly selected
    random sampling is done by using np.random.permutation() method
    np.random.seed(42) is used to generate the same random number for every run
    np.random.seed() is used to generate different random number for every run"""
    
    